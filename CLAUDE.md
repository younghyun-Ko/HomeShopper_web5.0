# HomeShopper (홈쇼퍼) 프로젝트 규칙

## 제품 정의
- 1인가구·빌라/원룸 시장 대상 부동산 풀서비스 플랫폼. 브라우저에서 동작하는 반응형 웹사이트
- 포지셔닝: '중개'가 아니라 '풀서비스 대리인'. 매물 탐색 → 임장 → 권리분석 → 계약 → 이사·인테리어·대출까지
- 모든 카피에서 2대 메시지 유지: (1) 중개부터 사후 서비스까지 풀서비스 (2) 중개 수수료 = 법정 상한 요율의 절반, 정찰제

## 레이아웃 원칙 (최우선 규칙)
- **데스크톱 우선 설계 (기준 뷰포트 1280px)**. 태블릿(768px)·모바일(390px)로 반응형 축소
- 콘텐츠 컨테이너: max-width 1200px 중앙 정렬 + 좌우 패딩 24px. 히어로/배너 배경은 full-bleed 허용
- 카드 리스트: 데스크톱 2~3열 그리드(gap 24px) → 태블릿 2열 → 모바일 1열
- 상세·대시보드 화면: 2단 레이아웃(예: 좌측 시각/갤러리 7 : 우측 정보/액션 5) → 모바일에서 세로 스택
- 폼/위저드 화면: 페이지는 넓게 두되 폼 자체는 중앙 640~720px 글래스 카드에 담는다
- 내비게이션: 상단 고정(sticky) 글래스 GNB가 기본 — 좌측 로고, 중앙/우측 메뉴(홈·위시리스트·임장·관리·서비스), 맨 우측 로그인/프로필. 모바일(<768px)에서만 하단 탭바로 전환
- 모달: 데스크톱은 중앙 모달(콘텐츠에 따라 max-width 640~880px, dim 배경). 모바일에서만 바텀시트로 전환
- 섹션 상하 여백 64~96px, 카드 간 24px. 모바일형 촘촘한 간격을 데스크톱에 쓰지 않는다
- 참고: docs/ 안의 와이어프레임 이미지는 '어떤 내용이 들어가는지' 참고용일 뿐, 그 좁은 폭·배치를 따라 하지 말 것

## 기술 스택 (변경 금지)
- Next.js 14 App Router + TypeScript(strict) + Tailwind CSS + lucide-react
- 상태: React Context + useReducer (전역 상태 라이브러리 도입 금지, MVP)
- 데이터: 모든 외부 호출은 src/lib/api/ 의 mock 함수를 통해서만. 컴포넌트에서 fetch 직접 호출 금지
- 배포 타깃: Vercel

## 디자인 시스템 — iOS Liquid Glass
- 브랜드 그라디언트: linear-gradient(135deg, #0083FF, #4C2CE2) — 주요 CTA, 활성 상태, 진행 바에만
- 컬러 토큰: --blue #0083FF · --purple #4C2CE2 · --ink #1A1A2E · --slate #5B6472 · --bg #F4F6FB · --success #1FB57A · --caution #E8A13A · --danger #E5484D
- 글래스 카드: background rgba(255,255,255,0.55) + backdrop-filter blur(24px) saturate(180%) + border 1px solid rgba(255,255,255,0.6) + border-radius 24px + box-shadow 0 8px 32px rgba(31,41,72,0.12) + inset 0 1px 0 rgba(255,255,255,0.7)
- 라운드: 카드 24px / 버튼 16px / 모달 24px / (모바일) 바텀시트 상단 28px
- 폰트: Pretendard(국문) + Inter(영문·숫자). 데스크톱 히어로 44~56px Bold, 섹션 타이틀 28~32px, 본문 16px, 캡션 13px
- 모션: 화면 전환 페이드+슬라이드업 220ms ease-out, 버튼 press scale-95 + opacity-90 (150ms), 모달 spring 등장, 리스트 stagger
- 클릭/터치 타겟 최소 44×44px, 모바일 하단 고정 요소는 env(safe-area-inset-bottom) 반영

## 공통 컴포넌트 (src/components/ui/) — 새 UI가 필요하면 반드시 재사용
GlassCard, GradientButton, Modal, SegmentControl, StepHeader, GlobalNav, PropertyCard, MatchBadge, Toast

## UX 라이팅
- 토스 스타일 '~해요체', 짧고 행동 중심 문장 (예: "조건을 남겨주시면 24시간 이내에 연락드려요")
- 버튼 라벨은 동사형 (예: "임장 예약하기", "서류 분석하기")

## 법적 표현 규칙 (절대 위반 금지)
- 서류 분석 결과에 점수·등급·'안심점수'·'안전 매물' 등 단정 표현 금지
- '유심히 봐야 할 포인트' / '확인이 필요한 항목' 형태로만 안내
- 모든 분석 결과 하단에 고지 고정: "본 안내는 참고용이며 법적 효력이 없습니다. 전문가 확인을 권장합니다."
- 수수료 문구는 "법정 상한 요율의 1/2, 정찰제"로 정확히 기재. 과장·확정적 안심 표현 금지
- 대출 화면에는 "실제 금리·한도는 금융사 심사에 따라 달라질 수 있습니다" 면책 고정

## 작업 방식
- 각 작업 완료 시 npm run build 로 타입/빌드 에러 없는지 확인 후 종료
- 커밋 메시지는 한국어 "feat: ..." 형식
